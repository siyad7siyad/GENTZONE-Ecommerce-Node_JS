<%- include('../layouts/adminHeader.ejs') %>

    <form action="/admin/addBrand" method="post" enctype="multipart/form-data" id="brand-form"
        onsubmit="return validationChecking()">
        <section class="content-main">
            <div class="row">
                <div class="col-9">
                    <div class="content-header">
                        <h2 class="content-title">Add New Brand</h2>
                    </div>
                </div>
                <div class="col-lg-6">
                    <div class="card mb-4">
                        <div class="card-header">
                            <h4>Basic</h4>
                        </div>
                        <div class="card-body">
                            <div class="mb-4">
                                <label for="brand_name" class="form-label">Brand name</label>
                                <input type="text" name="name" placeholder="Type here" class="form-control"
                                    id="brand_name">
                                <div class="error-message" style="color: red;" id="brand_name-error">

                                    <%# if(typeof(error)!="undefined" ){ %>
                                        <p>
                                            <%#=error%>
                                        </p>

                                        <%# } %>


                                </div>

                            </div>
                            <div class="mb-4">
                                <label class="form-label">Full description</label>
                                <textarea name="description" id="description" placeholder="Type here"
                                    class="form-control" rows="4"></textarea>
                                <div class="error-message" style="color: red;" id="description-error"></div>
                            </div>
                        </div>
                    </div> <!-- card end// -->
                </div>
                <div class="col-lg-3">
                    <div class="card mb-4">
                        <div class="card-header">
                            <h4>Picture</h4>
                        </div>
                        <div class="card-body">
                            <div class="input-upload">
                                <img src="" style="width:100px; height: 100px;" alt="" id="image-preview">
                                <input class="form-control" type="file" name="image"  id="brand_image" onchange="displayImage(this)">
                                <div class="error-message" style="color: red;" id="brand_image-error"></div>


                            </div>
                        </div>
                    </div> <!-- card end// -->
                    <div
                        <div class="card-body">

                            <div class="images-container">
                            
                            </div>
                     
                            
                        <input type="submit" value="submit" onchange="displayImage(this)" class="btn btn-md rounded font-sm hover-up">

                            <script>
                                // let imageCount = 0;

                                // function addImage() {
                                //     imageCount++;
                                //     const container = document.querySelector('.images-container');
                                //     const imageDiv = document.createElement('div');
                                //     imageDiv.classList.add('mb-4');
                                //     imageDiv.innerHTML = `
                                //         <label class="form-label">Image ${imageCount}</label>
                                //         <div class="image-container">
                                //             <img src="" alt="" id="image-preview${imageCount}">
                                //         </div>
                                //         <input class="form-control" name="image" type="file" onchange="displayImage(${imageCount}, this)">
                                //     `;
                                //     container.appendChild(imageDiv);
                                // }

                                function displayImage(input) {
                                    const preview = document.getElementById(`image-preview`);

                                    if (input.files && input.files[0]) {
                                        const reader = new FileReader();
                                        reader.onload = function (e) {
                                            preview.src = e.target.result;
                                        };
                                        reader.readAsDataURL(input.files[0]);
                                    } else {
                                        preview.src = '';
                                    }
                                }
                            </script>


                        

                    </div>
                </div>
            </div>
        </section> <!-- content-main end// -->
    </form>

    <!-- validate category form -->
    

                           
<script>

        document.addEventListener('DOMContentLoaded', () => {
            const brand_name = document.getElementById("brand_name")
            const brand_name_error = document.getElementById("brand_name-error")
            const description = document.getElementById("description")
            const description_error = document.getElementById("description-error")
            const brand_image = document.getElementById("brand_image")
            const brand_image_error = document.getElementById("brand_image-error")

            brand_name.addEventListener('input', () => {
                brand_name_error.innerHTML = brand_name.value.trim() === "" ? "please enter brand name" : ""
            })

            description.addEventListener('input', () => {
                description_error.innerHTML = description.value.trim() === "" ? "please enter brand description" : ""
            })

            brand_image.addEventListener('input', () => {
                brand_image_error.innerHTML = brand_image.value.trim() === "" ? "please add image" : ""
            })
        })

        function validationChecking() {
            const brand_name = document.getElementById("brand_name").value
            const description = document.getElementById("description").value
            const brand_image = document.getElementById("brand_image").value

            const brand_name_error = document.getElementById("brand_name-error")
            const description_error = document.getElementById("description-error")
            const brand_image_error = document.getElementById("brand_image-error")

            brand_name_error.innerHTML = brand_name.trim() === "" ? "please enter brand name" : ""
            description_error.innerHTML = description.trim() === "" ? "please enter brand description" : ""
            brand_image_error.innerHTML = brand_image.trim() === "" ? "please add image" : ""

            return !(brand_name.trim() === "" || description.trim() === "" || brand_image.trim() === "")

        }

  
    </script>







    <%- include('../layouts/adminFooter.ejs') %>